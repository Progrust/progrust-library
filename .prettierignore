node_modules
dist
.astro
package-lock.json
# リンクカードのOGPキャッシュ（生成物・コミット対象だがフォーマット対象外。link-card.md / deploy.md R-4）
.cache

# 既存の執筆済みプローズは Prettier の対象外にする。
# `check` は `prettier --check .`（全ツリー）なので、ユーザーが手で書いたドキュメント
# （docs/**/*.md やエスケープ済みコードフェンスを含む rule.md 等）を対象にすると
# format:check が失敗する。Prettier の対象は本プロジェクトが所有するコードに限定する。
content
docs
CLAUDE.md
.claude
