---
title: ダングリング参照
description: 参照先の値がすでに破棄・無効化された、有効な値を指していない参照。Rustでは借用チェッカーがコンパイル時に検出。
created_at: 2026-07-21
updated_at: 2026-07-21
tags: ["所有権"]
public: true
---

**ダングリング参照**（dangling reference）とは、参照先の値がすでに破棄されるなどして無効になった状態の[[reference]]のことです。C/C++などでは実行時のバグとして現れますが、Rustでは[[borrow]]の規則の1つとして「参照は常に有効な値を指していなければならない」と定められており、安全な（`unsafe`を使わない）Rustではダングリング参照が発生するコードはコンパイル時にエラーとして弾かれます。

<!-- rustc: expect E0515 -->
```rust playground
fn dangling<'a>() -> &'a String {
    let s = String::from("こんにちは");
    &s // エラー: E0515（ローカル変数 s への参照を返そうとしている）
} // ここで s はスコープを抜けて破棄される

fn main() {
    let r = dangling();
    println!("{r}");
}
```

## なぜ検出できるのか

上記の例では、関数`dangling`が返す参照は自身の内部で作られたローカルな[[variable]]`s`を指していますが、`s`は関数を抜ける際にスコープを抜けて破棄されます<!-- TODO: [[scope]] 作成後にリンク -->。つまり呼び出し元が受け取る参照は、すでに存在しない値を指すダングリング参照になってしまいます。

コンパイラはこれを、参照にライフタイム（参照が有効でいられる期間）を割り当てて検査することで検出します<!-- TODO: [[lifetime]] 作成後にリンク -->。上記の例では、両者のライフタイムの長さが噛み合わず、コンパイルエラーになります。

| | ライフタイム |
| --- | --- |
| 返り値の参照`'a` | 呼び出し元まで生き続ける必要がある |
| 参照先の`s` | 関数を抜けると同時に破棄される |

## 補足

:::details[ダングリング参照を避けるには]
この問題を安全に解決するには、値そのものの[[ownership]]を返す（`&String`ではなく`String`を返す）か、[[clone]]して独立した値を返すなど、関数を抜けた後も値自体が生き続けるようにする方法があります。
:::
