# T5-2 レビュー: クエリパーサ+フィルタ（純関数）

- **レビュー日**: 2026-07-11
- **対象コミット**:
  - `fae44ba` feat: 検索クエリパーサとフィルタ純関数を追加
  - `0df089b` docs: T5-2の実施履歴とチェックボックス更新

## DoDチェック結果

### 1. 完了条件・AC充足 — 合

- 完了条件（phase-5: search.md AC-3 / AC-4 / AC-5 相当のvitestが通る）を満たす。
- R-3: `parseQuery` が空白区切りでトークン化し `#`始まりをタグ・その他をキーワードに分解。`#`除去後に空のタグは捨てる妥当なエッジ処理。
- R-4: `matchesKeyword` がtitle/description/タグ名への部分一致を `toLowerCase` 比較で実装（大文字小文字非区別）。
- R-5: タグは `Array.includes` の完全一致で、部分一致（`Rust基礎` に対する `Rust`）を拾わない。
- R-6: `every` の組み合わせでAND検索。空条件は自動的に真＝空クエリ全件・入力順保持。
- 仕様にない実装の混入なし。未確定事項（全角半角・かなカナ同一視）は spec §5 のとおり初期実装対象外とし、大文字小文字の同一視のみに留めている。

### 2. テスト — 合

- `tests/scripts/search.test.ts` が §5 の配置規約（`src/scripts/search.ts` → `tests/scripts/`）に従い存在。implementation-rules §5 の配置例そのもの。
- AC-3/AC-4/AC-5 由来テストに `[AC-n]` 命名あり。AC-3はタイトル一致・概要一致・タグ名一致の3経路と大小非区別を、AC-4は部分一致除外を、AC-5はANDと片側条件のみの除外を検証。
- `parseQuery` 側（R-3。直接対応するACなし）も分解・連続/全角空白・`#`のみ・複数併記をカバー。
- describe/itは日本語の振る舞い文、AAA構造、純関数のためモックなし。いずれも規約準拠。

### 3. 検証コマンド — 合

- `npm run check`: format / lint / typecheck（0 errors）/ vitest 124件 すべて成功。
- `npx astro build`: 115ページの生成に成功。

### 4. ドキュメント反映 — 合

- phase-5 実施履歴に実装方針（純関数切り出し・`SearchEntry` の再利用・`/\s+/` 採用理由と `no-irregular-whitespace` 回避の知見）・テスト・検証・コミットが記録済み。
- search.md・architecture.md は既存記述で本実装をカバーしており、spec変更は不要。SSoT違反の重複記載なし。

### 5. コミット規約 — 合

- 両コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T5-2` トレーラーを持つ（誤ヒットなし）。

### 6. コード品質 — 合

- named exportのみ・kebab-caseファイル名・camelCase/PascalCase命名・`any`なし・export関数への日本語JSDoc、いずれも規約準拠。
- `SearchEntry` 型を T5-1 の `src/lib/search-index.ts` から import して再利用しており、型の重複定義がない。
- UI/DOM を含まない純関数のみを `src/scripts/search.ts` に置き、T5-3/T5-4 が共有する前提の切り出しは architecture §8・§10 と整合。

## 指摘一覧

**要修正**: なし

**推奨**: なし

**軽微**:

1. `src/scripts/search.ts:55` — タグの照合（`entry.tags.includes(tag)`）は大文字小文字を**区別する**。R-5は「タグ名の完全一致」とのみ規定し大小の扱いに言及がないため仕様準拠だが、キーワード（R-4: 非区別）と非対称になる。`#rust` で `Rust` タグを拾いたくなった場合は、spec（search.md R-5）を先に更新してから実装変更すること。
2. `tests/scripts/search.test.ts:107`（空クエリテスト）— `toHaveLength(3)` のみで、JSDocが謳う「入力順を保持する」ことは未検証。他のテストが `map(url)` の `toEqual` で順序も見ているため実質カバーされているが、このケースも同形式にするとより厳密。

## 総合判定

**承認**
