# T4-1 レビュー: embedパーシャル

- **レビュー日**: 2026-07-10
- **対象コミット**:
  - `e51d2cf` feat: 辞書embedパーシャルを追加
  - `883972e` docs: T4-1の進捗と実施履歴を記録

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | ✅ | AC-6 を生成物レベルで確認: `dist/dict/<slug>/embed/index.html` が全公開辞書 23 件で生成され、非公開の `error-handling`・`mutex` は辞書ページ・embed とも未生成（`content/dict` 全 25 件と `dist/dict` の突合で差分が非公開 2 件のみ）。断片は `<html>`/`<head>`/ヘッダー/ナビを含まず、ルート `[data-dict-embed]` + `data-slug`/`data-title`/`data-tags` + `.prose` 本文のみで architecture §5 の新設コントラクトと一致。断片内の wikilink も `data-dict-link` 付きで出力されており R-6（ペイン内リンクの再拡張）の前提を満たす。spec §3 が「詳細構造は実装時に architecture.md で定義」と委譲していた事項の確定であり、仕様外の実装なし |
| 2 | テスト | ✅（対象外として妥当） | `[AC-6]` 命名の vitest は追加されていないが、architecture §10 が「ページ・UIは自動テストせず、ビルド成功と目視で確認する」と定めており、embed.astro はページ（getStaticPaths + render のみでロジックなし）。公開フィルタ自体は既存 `tests/lib/content.test.ts` でカバー済み。実施履歴にも §10 準拠の判断が明記されている |
| 3 | 検証コマンド | ✅ | レビュー時に実行: `npm run check`（format:check / lint / typecheck 0 errors・0 hints / vitest 11 files・89 tests）PASS、`npx astro build` 成功（115 ページ） |
| 4 | ドキュメント反映 | ✅ | embed 断片の構造コントラクトを `docs/architecture.md` §5 に追記（spec §3 からの委譲先として正しい配置）。data 属性設計・consumer 側の組み立て方針（プレビュー=R-7 / ペイン=R-11）・パイプ区切り規約・CSS 非同梱の前提まで定義されており、T4-2/T4-3 が参照できる。`docs/plan/phase-4-wikilink-ui.md` にチェックボックス更新と実施履歴（変更ファイル・設計判断・検証）。embed.astro 側のコメントは「architecture.md §5 を正とする」と参照で済ませており SSoT 違反の重複なし |
| 5 | コミット規約 | ✅ | 2 コミットとも Conventional Commits（feat/docs）+ 日本語サブジェクト + 正確な `Task: T4-1` トレーラー |
| 6 | コード品質 | ✅ | `getStaticPaths` が `getPublicDict()` 経由（architecture §5 の必須ルール）で、既存 `dict/[slug].astro` と同一パターン。日本語コメントで「なぜ」（partial の目的・非公開除外の根拠・data 属性設計の理由）を説明。`any` なし・命名準拠・`<style>` なし。`data-tags` のパイプ区切りは `DictCard`/`EntryCard` の既存規約と一致 |

## 指摘一覧

### 要修正

なし。

### 推奨

なし。

### 軽微

- **M-1: タグ名に `|` を含む場合の `data-tags` 破綻**（`src/pages/dict/[slug]/embed.astro:34`） — `tags.join("|")` はタグ名自体に `|` が含まれると consumer 側のパースが崩れる。これは T3-6 M-3 で既に指摘済みの既存規約（`DictCard` と同一）の踏襲であり本タスク起因ではないが、embed で参照箇所が増えたため、content-model.md へのタグ名文字制約の追記（T3-6 M-3 と同一対応）で一括解消するのが望ましい。現コンテンツに該当タグはなく実害なし。
- **M-2: 断片単体アクセス時の見た目は保証されない** — 断片が `global.css` を読み込まない設計（architecture §5 に明記済み）のため、`/dict/[slug]/embed/` をブラウザで直接開くと素の HTML になる。fetch 専用 URL であり仕様上問題ないが、consumer 実装（T4-2/T4-3）で挿入先に `.prose` スタイルが確実に届いていることの確認が前提になる点のみ申し送り。

## 総合判定

**承認**。AC-6 を生成物レベルで確認（公開 23 件生成・非公開 2 件除外・断片構造がコントラクト通り）、DoD 全項目クリア。軽微 2 件はいずれも対応任意で、M-1 は T3-6 M-3 と併せた対応が自然。
