# T1-2 レビュー: 4コレクション定義と公開フィルタ

- レビュー日: 2026-07-09
- レビュー方式: `/task-review T1-2`（`git log --grep "Task: T1-2"` によるコミット収集 + 範囲diff `4666c2a^..2dad2ca`）

## 対象コミット

| ハッシュ | サブジェクト |
| --- | --- |
| 4666c2a | feat: 4コレクション定義とID導出ロジックを追加 |
| aad73fc | feat: 公開フィルタと並び順ユーティリティを追加 |
| 8043237 | chore: エントリ確認用の仮ページを追加 |
| 0472050 | docs: T1-2の実施履歴と進捗を記録 |
| 2dad2ca | refactor: zodのzを非推奨のastro:contentからastro/zodへ変更 |

全5コミットの本文に正確なトレーラー行 `Task: T1-2` を確認済み（誤ヒットなし）。

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | **合格** | content-model AC-3（dict/articles=ファイル名のみID）・AC-4（連番除去URL + 章順）を `collection-id.ts` + vitest で充足。AC-1 は実施履歴に手動検証記録あり（`title` 削除→ファイル名付きビルド失敗を確認）。公開フィルタは AC-6〜AC-8 相当のロジックテストで充足。R-9/R-6 の throw は T1-4 スコープと明記されており plan と整合。仕様にない実装なし（`debug-entries.astro` は plan 記載の「確認用の仮ページ」で P3 撤去が明記されている） |
| 2 | テスト | **合格** | `tests/lib/collection-id.test.ts`（[AC-3]×3・[AC-4]×3）、`tests/lib/content.test.ts`（[AC-6]・[AC-7]・[AC-8]×2）。配置は `tests/` ミラー構成、describe=モジュール名・it=日本語振る舞い文、AAA、`toMatchObject` 部分一致、純関数はモックなし — §5 準拠。軽微指摘#2参照 |
| 3 | 検証コマンド | **合格** | レビュー時に実行: `npm run check`（format:check / lint / typecheck=0 errors・0 warnings・0 hints / vitest 14件）green、`npx astro build` 成功（2ページ生成） |
| 4 | ドキュメント反映 | **合格（推奨1件）** | phase-1-foundation.md に対応概要・検証結果・申し送りを追記、T1-2 チェックボックス完了化。実装は architecture §2 の設計（loader/generateId/スキーマ/公開フィルタ API 名）と一致し重複記載なし。ただし `astro/zod` import 規約の置き場所に改善余地（推奨指摘#1） |
| 5 | コミット規約 | **合格** | 5件すべて Conventional Commits（feat/chore/docs/refactor）+ 日本語サブジェクト + `Task: T1-2` トレーラー。意味のある単位で分割されている |
| 6 | コード品質 | **合格** | named export のみ・kebab-case ファイル名・camelCase 識別子・`any` なし・コメント/JSDoc 日本語（export 関数に1行JSDocあり）・「なぜ」コメント（load-order リスク回避の理由等）。`collection-id.ts` を astro:content 非依存の純モジュールに切り出した判断は妥当。エッジケース（filePath 欠落・連番なし章）もテスト済み |

## 指摘一覧

### 要修正

なし。

### 推奨

1. **`astro/zod` import 規約を `docs/implementation-rules.md` §3 に反映する**（`docs/plan/phase-1-foundation.md:57`）
   「zodの`z`は`astro/zod`からimportする（`astro:content`再エクスポートは@deprecated）」は今後全タスクに効くコーディング規約だが、現在は T1-2 の実施履歴（申し送り）にのみ記録されている。plan の実施履歴は将来のセッションで必ず読まれるとは限らず、implementation-rules.md が「どう書くか」のSSoTなので、§3 に1行追加し plan 側は参照に切り替えるのが望ましい（SSoT原則・知見の還流）。

### 軽微

2. **`sortChapters` の連番昇順テストに `[AC-4]` タグがない**（`tests/lib/content.test.ts:82`）
   content-model AC-4 の後半「章目次で `01-*.md` の次に並ぶ」は `sortChapters` が担っており、`it("元ファイル名先頭の連番昇順に並べる")` はAC由来テストに該当する。§5 の AC対応付け規約（grep 追跡）に従い `[AC-4]` を先頭に付けるとよい。

3. **`chapterOrder` の連番欠落時 0 フォールバックは暫定挙動**（`src/lib/content.ts:76`）
   連番のない章が黙って先頭に並ぶが、T1-4 で R-9（連番なし章は throw）が入れば到達しなくなる前提。テストで挙動が固定されており現時点で問題なし。T1-4 実装時にこのフォールバックの要否を見直すこと。

## 総合判定

**承認**（要修正なし。推奨1件・軽微2件は後続タスクで対応可）
