# T3-2 レビュー: トップページ

- レビュー日: 2026-07-09
- 対象コミット（`Task: T3-2` トレーラー確認済み・誤ヒットなし）:
  - `0aeed51` feat: 新着一覧のマージ純関数 mergeRecent/withKind を追加
  - `01bc084` feat: トップページを実装（新着台帳・一覧導線・SNS/プロフィール）
  - `2705b6d` docs: T3-2の進行状況と実施履歴を記録

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | **不合格（1点のみ）** | 完了条件は「pages.md AC-2 を満たし、top-b-sharpモックと目視一致」。AC-2 は充足を確認: `mergeRecent` が辞書・記事・本のみを結合し `sortByNewest`（created_at降順・同日タイトル昇順=R-4）→ `slice(0, 10)`。章は引数に取らない設計で構造的に除外され、ビルド成果物 `dist/index.html` でも新着 `<li>` 10件・本リンクは本トップ `/books/rust-web-app-book` のみ（章URL混入0件）を確認。R-6（4一覧への件数付きリンク）・R-7（SNSアイコン+プロフィールボタンのみ、プロフィール本文なし）も充足。仕様にない実装の混入なし。**ただしモック照合で1点相違を発見**: コレクション導線の縦罫線ロジックがモバイル（lg未満の2列グリッド）でモックと一致しない（要修正F-1）。それ以外（ヒーロー・台帳リスト行・バッジ配色）はモック `top-b-sharp.html` およびui-design-spec「コンテンツ種別バッジ」「台帳リスト行」の定義とクラスレベルで一致（配色トークンのCSS変数方式による `dark:` 省略は architecture.md §9 準拠で相違に当たらない） |
| 2 | テスト | 合格 | `tests/lib/content.test.ts` に `mergeRecent` の `[AC-2]` テスト3件+上限テスト1件を追加。配置（`src/lib/content.ts` → `tests/lib/content.test.ts`）・日本語it名・AAA・モックなし（純関数）はすべて§5準拠。ページ/UI部分をvitest対象外とした判断は architecture.md §10 と整合し、理由も実施履歴に明記。軽微指摘2件（M-1: 上限テストの `[AC-2]` プレフィックス漏れ、M-2: 章除外テストの検証力） |
| 3 | 検証コマンド | 合格 | レビュー時に実行: `npm run check`（format:check / lint / typecheck / vitest 70件）PASS、`npx astro build` 成功（`/index.html` 生成） |
| 4 | ドキュメント反映 | 合格 | phase-3-pages.md のチェックボックス・実施履歴（実装内容・AC・テスト方針・検証結果・コミット一覧）を記録。KindBadge の配色はui-design-specの表に従った実装でありSSoT違反の重複記載ではない。spec変更を要する知見なし |
| 5 | コミット規約 | 合格 | 3コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T3-2` トレーラー（`--grep` は3件のみヒット、各本文のトレーラー行を個別確認。`T3-1` の検索汚染なし）。純関数+テスト→ページ実装→docs の分割も§6「意味のある単位ごと」に適合 |
| 6 | コード品質 | 合格 | named exportのみ・`content.ts` はkebab-case・`KindBadge.astro` はPascalCase・`any` 不使用（`mergeRecent` はジェネリクス3型+ユニオン戻り値で型安全）・`interface Props` + 分割代入・frontmatter記述順（import→Props→取得→ロジック）遵守・日本語JSDoc/コメントは「なぜ」中心（章を引数に取らない設計意図、タグ件数インライン集計の理由など良質）。`withKind`/`mergeRecent` は既存 `sortByNewest` を再利用し重複なし。タグ件数が本・章を含むのはR-18/R-19（タグページは章も対象）との整合として妥当。`toISOString().slice(0, 10)` はfrontmatterの日付のみ`created_at`（UTC深夜0時）に対して安全 |

## 指摘一覧

### 要修正

- **F-1** `src/pages/index.astro:163-164` — コレクション導線の縦罫線がモバイル/タブレット（lg未満・2列グリッド）でモックと相違する。実装は `i !== collections.length - 1 && "border-r border-line"` のため2番目のセル（記事）にも全幅で `border-r` が付くが、モック `top-b-sharp.html` では記事セルは `lg:border-r`（4列時のみ）で、2列表示では罫線なし。結果、lg未満で1行目（辞書|記事）の右端にだけ縦罫線が出て2行目（本|タグ）の右端には出ない非対称な線が現れ、完了条件「top-b-sharpモックと目視一致」を満たさない。加えて165行目の `i === 1 && "lg:border-r"` は163行目が常時 `border-r` を付けるため死にコード（本来この行が効くべき設計だったことを示唆）。修正案: `(i === 0 || i === 2) && "border-r"` + `i === 1 && "lg:border-r"` とし `border-line` は常時付与（モックと同構成）。

### 推奨

- なし

### 軽微

- **M-1** `tests/lib/content.test.ts:126` — `it("11件以上でも先頭 limit 件（既定10件）に絞られる")` に `[AC-2]` プレフィックスがない。AC-2 は「最大10件」を明示しており、このテストはAC由来（§5「specの受入基準に由来するテストは it名の先頭にAC番号」）。grepでのspec対応追跡から漏れるため `[AC-2]` を付けるのが望ましい。
- **M-2** `tests/lib/content.test.ts:96-108` — 「章は引数に取らない設計で、kind は辞書・記事・本のみになる」テストは、章を渡さずに入力3件がそのまま返ることを見るだけで、章の除外自体は検証していない（シグネチャ上章を渡せない以上テスト不能なのは事実）。テストの実効性はビルド成果物確認（章URL混入0件）が担っており、現状でも許容範囲。it名を実際の検証内容（kindの網羅）に寄せるとより正確。

## 総合判定

**要修正**（F-1 の1点のみ。AC-2・テスト・検証コマンド・規約はすべて合格であり、修正は罫線条件の1行変更で完了する見込み）

---

# T3-2 再レビュー（レビュー指摘対応の確認）

- レビュー日: 2026-07-09
- 対象コミット（`Task: T3-2` トレーラー確認済み・誤ヒットなし。前回3件 + 修正1件）:
  - `0aeed51` feat: 新着一覧のマージ純関数 mergeRecent/withKind を追加
  - `01bc084` feat: トップページを実装（新着台帳・一覧導線・SNS/プロフィール）
  - `2705b6d` docs: T3-2の進行状況と実施履歴を記録
  - `a8d19d5` fix: トップのコレクション導線の縦罫線をモックに合わせる（初回レビューF-1/M-1/M-2対応）

## 指摘対応の確認

| 指摘 | 判定 | 根拠 |
| --- | --- | --- |
| F-1（コレクション導線の縦罫線） | **解消** | `src/pages/index.astro` を `(i === 0 \|\| i === 2) && "border-r"` + `i === 1 && "lg:border-r"`・`border-line` 常時付与に修正。生成HTMLの4リンクのクラスが `border-r` / `lg:border-r` / `border-r` / なし となり、モック `top-b-sharp.html` のパターンと完全一致（lg未満の2列表示でも列間のみに罫線）。死にコードだった `i === 1 && "lg:border-r"` は本来の役割で機能するようになり、コメントも実装意図を正しく説明 |
| M-1（上限テストの `[AC-2]` 漏れ) | **解消** | `it("[AC-2] 11件以上でも先頭 limit 件（既定10件）に絞られる")` にプレフィックス付与 |
| M-2（章除外テストのit名） | **解消** | `it("[AC-2] kind は辞書・記事・本の3種のみで、章は結合対象に含まれない")` へ変更し、実際の検証内容（kindの網羅）と一致 |

## DoDチェック結果（差分再確認）

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | 合格 | 前回不合格の唯一の相違（F-1）が解消し、モック照合で残る相違なし。AC-2は前回確認どおり充足 |
| 2 | テスト | 合格 | M-1/M-2反映済み。`mergeRecent` のAC由来テスト4件すべて `[AC-2]` 命名 |
| 3 | 検証コマンド | 合格 | 再レビュー時に実行: `npm run check`（vitest 70件）PASS、`npx astro build` 成功 |
| 4 | ドキュメント反映 | 合格 | 修正コミット本文に指摘番号と対応内容を記録。spec/plan文書への追加反映を要する知見なし |
| 5 | コミット規約 | 合格 | `a8d19d5` は fix + 日本語サブジェクト + 正確な `Task: T3-2` トレーラー |
| 6 | コード品質 | 合格 | 修正差分に規約違反なし。罫線条件は列位置を明示する定数比較になり可読性も向上 |

## 指摘一覧

- なし

## 総合判定

**承認**（初回レビューの要修正1件・軽微2件すべて解消。DoD全項目合格）
