# T3-1 レビュー: BaseLayout + ヘッダー/フッター + テーマ切替

- レビュー日: 2026-07-09
- 対象コミット（`Task: T3-1` トレーラー確認済み・誤ヒットなし）:
  - `39e6d86` feat: カラートークン・フォント・Shiki切替CSSのデザインシステムを追加
  - `3f24a27` feat: BaseLayout・ヘッダー/フッター・テーマ切替を追加
  - `68e6439` docs: index.astroをBaseLayoutへ移行しT3-1の進行状況を記録

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | 合格 | 完了条件は theme.md AC-1〜AC-4。コードが要求仕様を満たすことを確認: R-1（FOUCスクリプトが `localStorage.theme` 優先・未保存時 `prefers-color-scheme`、切替時に `theme.ts` が保存）、R-2（`html.classList.toggle('dark')` + `@custom-variant dark`）、R-3（`BaseLayout.astro` の `<head>` 内 `is:inline` 同期スクリプト。ビルド成果物 `dist/index.html` への埋め込みも確認）、R-4（Header に ThemeToggle 配置）、R-5/AC-4（Shiki切替CSSが shiki.md 95〜103行のレシピと完全一致・`.astro-code` セレクタ、mermaid は既存の `dark:` 駆動 div と `.dark` クラストグルで連動）。カラートークン7色×2テーマ・フォント3種は ui-design-spec.md の表と値レベルで完全一致。ダークを `n` 接頭辞トークンでなく同名CSS変数差し替えにした点は architecture.md §9 が採用を明記する方式で spec の info 注記とも整合。Header（ロゴ+検索+テーマ切替の3点のみ・sticky・`bg-paper/95 backdrop-blur border-line/70`）は pages.md R-2 と ui-design-spec のマークアップに一致、Footer（mono コピーライト + Profile/GitHub/X/RSS・`border-t border-line/70`）は R-3 に一致。SearchBox はタスク記述どおり見た目のみ（`disabled` + 準備中 aria-label）。仕様にない実装の混入なし。AC のブラウザ挙動自体は実施履歴の Playwright 検証報告（AC-1〜AC-4 全PASS）に依拠 |
| 2 | テスト | 合格（新規テストなしを妥当と判断） | `[AC-n]` テストは未追加だが、architecture.md §10 が「ページ・UIは自動テストせず、ビルド成功と目視で確認。E2Eは導入しない」と明記し、§8 のvitest対象も `search.ts`（純関数）のみで `theme.ts` は対象外。理由は実施履歴に明記されており（テスト方針の節）、規約と矛盾しない。既存66テストはすべてgreen |
| 3 | 検証コマンド | 合格 | レビュー時に実行: `npm run check`（format:check / lint / typecheck / vitest 66件）PASS、`npx astro build` 成功（3ページ生成） |
| 4 | ドキュメント反映 | 概ね合格 | astro.config.mjs の「html.dark切替CSSはP6」旧コメントを実装済みへ修正、phase-3-pages.md のチェックボックス・実施履歴（実装内容・テスト非追加の理由・検証結果）を記録。SSoT違反の重複記載なし。ただし実施履歴が存在しないファイルを参照（推奨R-1）、shiki.md の「未目視」注記が未更新（軽微M-1） |
| 5 | コミット規約 | 合格 | 3コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T3-1` トレーラー（`--grep` で3件のみヒット、本文トレーラー行を個別確認）。68e6439 の type 選択のみ軽微指摘（M-2） |
| 6 | コード品質 | 合格 | named export のみ・`src/scripts/`/`src/lib/` は kebab-case（`theme.ts`・`site.ts`）・コンポーネントは PascalCase・`any` 不使用・`interface Props` + 分割代入・日本語JSDoc/コメント（「なぜ」中心。`@theme inline` を使わない理由コメントは特に良い）。クライアントJSは `theme.ts` に分離され ThemeToggle の `<script>` は import+init のみ、FOUC インラインは規約 §4 が認める唯一の例外に該当。`initThemeToggle` はボタン不在時に安全に return。ui-design-spec 共通ルールの `transition-colors motion-reduce:transition-none` 併記も全ホバー要素で遵守 |

## 指摘一覧

### 要修正

- なし

### 推奨

- **R-1** `docs/plan/phase-3-pages.md:34` — 実施履歴が「設計方針の詳細は [`.claude/plans/t3-1-pure-mccarthy.md`] を参照」とするが、このファイルはリポジトリに存在しない（`.claude/plans/` ディレクトリ自体がなく、コミットもされていない）。将来の読者には辿れない死リンク。要点は履歴本文に記載済みなので参照文を削除するか、残すならファイルをコミットする。

### 軽微

- **M-1** `docs/markdown-pipeline/shiki.md:140` — 「実ブラウザでの `html.dark` トグル時の見た目切替は未目視（本実装でスタイルを組んだ時点で1度ブラウザ確認する）」の注記が、本タスクの Playwright 検証（AC-4 PASS）で解消済みなのに未更新。DoD 5（知見の還流）として検証済みへ更新するのが望ましい。
- **M-2** コミット `68e6439` — type が `docs:` だが `src/pages/index.astro` の BaseLayout 移行というコード変更を含む。厳密には refactor/feat 相当の変更が混在。実害はないが、今後はコード変更とドキュメント更新で type を分けるか、主たる変更に合わせた type を選ぶとよい。
- **M-3** `src/lib/site.ts:14` — RSS リンク `/rss.xml` はフィード実装（P6）まで 404 になる。pages.md R-3 どおりリンク設置自体は正しく暫定URLの方針も履歴に明記済みだが、P6 まで死リンクである点は認識事項として記録する。

## 総合判定

**承認**（要修正なし。推奨1件・軽微3件は次回以降のdocsコミットで対応すれば十分）
