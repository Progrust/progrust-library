# T5-1 レビュー: 検索インデックス生成

- **レビュー日**: 2026-07-11
- **対象コミット**:
  - `bf054b8` feat: 検索インデックス生成を追加
  - `19d1022` docs: T5-1の実施履歴とチェックボックス更新

## DoDチェック結果

### 1. 完了条件・AC充足 — 合

- 完了条件（phase-5: search.md AC-1を満たし変換ロジックのvitestが通る）を満たす。
- AC-1をビルド成果物 `dist/search-index.json` の実測で確認: 全33エントリ（dict 23 / article 6 / book 1 / chapter 3）。章エントリが含まれ、非公開辞書 `mutex`・`error-handling` のエントリは不在。全エントリが search.md §3 のスキーマ（title/description/tags/type/url）に適合。
- URL導出の整合を確認: dict `/dict/[slug]`・article `/articles/[slug]`・book `/books/[slug]`（本トップ）・chapter `/books/[本slug]/[章slug]`（章詳細）は実在ページのルーティング（`src/pages/`）および `collection-id.ts` のID規則と一致。章URL（例 `/books/rust-web-app-book/environment`）に対応する dist ページの存在も確認。
- 仕様にない実装の混入なし。eslint globals オーバーライドはツールチェーン配線であり仕様外機能の追加ではない。

### 2. テスト — 合

- `tests/lib/search-index.test.ts` が §5 の配置規約（`src/lib/search-index.ts` → `tests/lib/`）に従い存在。
- AC-1由来テストに `[AC-1]` 命名あり（4種別のtype/URL変換・章エントリ含有）。
- describe/itは日本語の振る舞い文、AAA構造、`toMatchObject`/`toEqual` の使い分けも規約通り。
- 非公開除外は `getPublic*`（`content.test.ts` が担保）へ委譲する設計で、純関数側で重複検証しない方針がテストコメントに明記されている。architecture §10（vitest対象は純関数）と整合。

### 3. 検証コマンド — 合

- `npm run check`: format / lint / typecheck（0 errors）/ vitest 114件 すべて成功。
- `npx astro build`: 115ページ + `search-index.json` の生成に成功。

### 4. ドキュメント反映 — 合

- phase-5 実施履歴に実装構成・テスト・検証・知見（eslint globals の件と implementation-rules §1 へ追記不要とした判断根拠）が記録済み。
- architecture §5/§7・search.md は既存記述で本実装をカバーしており、spec変更は不要。SSoT違反の重複記載なし。

### 5. コミット規約 — 合

- 両コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T5-1` トレーラーを持つ（`T5-1x` 等への誤ヒットなし）。

### 6. コード品質 — 合

- named exportのみ・kebab-caseファイル名・camelCase/PascalCase命名・`any`なし・`src/lib/` export関数への日本語JSDoc、いずれも規約準拠。
- `wikilink-graph.ts` と同型の「純関数 + `getPublic*` 集約ラッパ」構成で architecture §7 の指定（生成ロジックを `src/lib/search-index.ts` に置きvitest対象とする）通り。
- エンドポイントのファイル名 `search-index.json.js` も architecture §5 の既定名と一致。

## 指摘一覧

**要修正**: なし

**推奨**: なし

**軽微**:

1. `src/lib/search-index.ts:36`（`urlFor`）— 種別→URLの導出規則が `content.ts` の `chapterLedger`（`/books/${bookSlug}/${slug}`）や各ページのリンク組み立てと分散して重複している。現状は各所にコメントで対応関係が明記されており実害はないが、URL規則を変更する際は横断修正が必要になる点に留意（将来リファクタ候補）。

## 総合判定

**承認**
