# T2-2 レビュー: shiki（diff・ファイル名・dual theme）

- レビュー日: 2026-07-09
- レビュアー: Claude Fable 5（/task-review）

## 対象コミット

| ハッシュ | サブジェクト |
| --- | --- |
| 006dc31 | feat: shikiでdiff・ファイル名・dual themeを組み込む |
| 0ff01bc | test: code-filenameプラグインのテストを追加 |
| 4bef18e | docs: T2-2の実施結果を記録しshiki.mdの残課題を解消 |

3コミットとも本文に正確な `Task: T2-2` トレーラー行を確認済み（誤ヒットなし）。範囲diff `git diff 006dc31^..4bef18e` で全体を確認した。

## DoDチェック結果

1. **完了条件・AC充足: 合格** — コードハイライトに直接対応する `docs/spec/` の受入基準は存在せず（grepで確認。theme.md AC-4「テーマ切替でハイライト配色が切り替わる」はタスク説明が「確定配色対応はP6のフォント/テーマタスクで」と明示的にP6へ割り当て済み）。完了条件はplan文書（phase-2 T2-2）の「` ```rust:main.rs `のファイル名タブ、`[!code ++/--]`のdiffクラス付与、light/dark両テーマのハイライトがビルド結果に含まれる」。実装は shiki.md の検証済み方式（`shikiConfig` を `markdown` 直下・`transformerNotationDiff`・mdast前処理 `codeFilename` を `mdastPlugins` 先頭）に忠実で、仕様にない実装の混入なし。dual themeの `html.dark` 切替CSS未実装（P6までコード無彩色）は plan 実施履歴に「記録済みの判断」として明記されており、完了条件の文言（ビルド出力に両テーマの `--shiki-light`/`--shiki-dark` 変数が含まれる）は満たす。レビュー時に現dist（`dist/debug-render/index.html`）で `has-diff`・`diff add`・`diff remove`・`--shiki-light`/`--shiki-dark`（各310箇所）・`data-language="rust"`（plaintextフォールバックなし）を独立確認した（指摘R-1の注記あり）
2. **テスト: 合格（注記あり）** — `tests/plugins/code-filename.test.ts` 4ケースが §5 の規約（`tests/` ミラー配置・`.test.ts`・日本語it名・AAA・`tests/helpers/` の共通ヘルパー・`toContain` による部分一致は既存wikilink/directivesテストと一貫）に従う。**`[AC-n]` 命名はspecに対応ACが存在しないため対象外**（T2-1レビューと同じ判断）。Shiki本体は `markdownToHtml` 直接呼び出しでは走らないためユニットテストの範囲がmdast前処理に限られる制約は、ヘルパーのJSDocと plan 実施履歴の両方に明記されており妥当
3. **検証コマンド: 合格** — レビュー時に実行し `npm run check`（format:check / lint / typecheck / test 40件）green、`npx astro build` 成功（3ページ）を確認
4. **ドキュメント反映: 合格** — shiki.md の残課題「コンテンツコレクション経由の実ビルドは未検証」を検証済みへ更新し、雛形コードの名前を本番名（`codeFilename` / `code-filename.mjs`）へ揃え、mermaidの plaintext フォールバック（T2-4で対応）という新知見も還流済み。phase-2 実施履歴に検証手順（debug-render の targetIds 一時拡大→dist grep→revert）まで記録されており再現可能。SSoT違反の重複なし（実施履歴とshiki.mdの検証結果記載は記録と知見文書で役割が異なり許容範囲）
5. **コミット規約: 合格** — 3コミットとも Conventional Commits + 日本語サブジェクト + `Task: T2-2` トレーラー。type（feat/test/docs）の使い分けも適切で、意味のある単位（実装/テスト/ドキュメント）に分割されている
6. **コード品質: 合格** — `// @ts-check` + JSDoc型注釈、named exportのみ、kebab-caseファイル名、`any` なし（satteriの型が `hName`/ブロック子を許容しない箇所は `unknown` 経由のキャストで橋渡し、§3準拠）、コメントは「なぜ」（plaintextフォールバックの理由・hast側では手遅れの理由）を日本語で記述。最初のコロンのみで分割するためパス付きファイル名（`src/main.rs`）も正しく扱え、置換後の新codeノードはコロンを含まないため再訪問しても早期returnとなり無限ループの懸念なし。`@shikijs/transformers@4.3.1` はAstro同梱shiki 4.3.1と完全一致（lockfileで確認）

## 指摘一覧

### 要修正

なし。

### 推奨

なし。

### 軽微

- **R-1: ファイル名記法がリポジトリの既定ビルドでは継続的に検証されない**（`src/pages/debug-render.astro:15`）— 完了条件のうちdiff・dual themeは現distで恒常的に確認できるが、` ```lang:file ` 記法を含むエントリ（`axum-web-api-intro` 等）は既定の `targetIds` に含まれず、`code-filename` は現distに0件（検証は一時拡大→revertの単発）。mdast前処理はユニットテストで担保されており、T2-5（全記法を含むテスト用mdの全プラグイン同時動作確認）で恒常カバーされる予定のため軽微とする。T2-5のテスト用mdに ` ```rust:main.rs ` を必ず含めること
- **R-2: lang部分が空（` ```:main.rs `）のエッジケースが未処理・未テスト**（`plugins/code-filename.mjs:17`）— `realLang` が空文字列でもラップして `lang: ""` のcodeノードを生成する。rule.mdの記法上は言語必須のため実害は低い（Shikiはplaintext相当へフォールバックする見込み）が、空ファイル名（` ```rust: `）は素通りガードがあるのに対し非対称。次にこのプラグインへ触れる際に `!realLang` の素通りガード追加かテストでの挙動固定を検討するとよい

## 総合判定

**承認**（要修正なし）。実装は shiki.md の検証済み方式に忠実で、DoD 6項目すべて合格。軽微2件はいずれも後続タスク（T2-5・次回プラグイン修正時）で吸収可能。
