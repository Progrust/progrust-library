# T2-3 レビュー: link-card

- レビュー日: 2026-07-09
- レビュアー: Claude Fable 5（/task-review）

## 対象コミット

| ハッシュ | サブジェクト |
| --- | --- |
| b2a7dfd | feat: link-cardプラグインを追加しパイプラインへ登録 |
| 1fcde0a | test: link-cardプラグインのテストを追加 |
| b6416b8 | docs: link-cardの雛形逸脱と実ビルド検証結果を記録しT2-3を完了 |
| 0cfb37c | test: link-cardのblock要素開始（落とし穴1）の回帰ガードを追加 |

4コミットとも本文に正確な `Task: T2-3` トレーラー行を確認済み（誤ヒットなし）。範囲diff `git diff b2a7dfd^..0cfb37c` で全体を確認した。

## DoDチェック結果

1. **完了条件・AC充足: 合格** — link-cardに直接対応する `docs/spec/` の受入基準は存在せず（grepで確認。deploy.md R-4「キャッシュはリポジトリにコミットしてビルド間で再利用」「CI上のfetch失敗はビルドエラーにしない」は要求仕様であり、対応AC-1はCI/デプロイ全体のACでP6の範疇）。完了条件はplan文書（phase-2 T2-3）の「ダミーコンテンツのベアURLがカード化され、キャッシュファイルが生成され、fetch失敗時もビルドが成功して簡易カードになる」+ タスク説明の「内部リンク除外ガードを入れる」。実装は link-card.md の検証済み雛形に基づき、逸脱3点（命名・コミット可キャッシュパス・成功のみ保存）はいずれも根拠（architecture.md §1既定名 / deploy.md R-4 / 恒久フォールバック回避＝ユーザー確認済み）が明示され、優先順位ルール（spec > markdown-pipeline）に従い先にlink-card.mdが更新されている。仕様にない実装の混入なし。カード化の検出条件（段落の唯一の子・text===url）は rule.md「リンクカード」の記法定義と一致。到達可能URLのカード化・`.cache/link-card/ogp.json` 生成・到達不能URLのフォールバック+build exit 0 は実 `astro build` のdistで確認済みと実施履歴に記録され、レビュー時にも committed cache（実OGPデータ4件）とビルド成功を独立確認した
2. **テスト: 合格** — `tests/plugins/link-card.test.ts` 8ケースが §5 の規約に準拠: `tests/` ミラー配置・`.test.ts`・日本語it名・AAA・`tests/helpers/link-card.ts` の共通ヘルパー・部分一致アサーション・**fetchの `vi.stubGlobal` スタブ（§5に明記された方式そのもの）**。`[AC-n]` 命名はspecに対応ACが存在しないため対象外（T2-1/T2-2レビューと同じ判断）。テストごとの scratch cacheDir（mkdtemp→rmSync）で実キャッシュ汚染・テスト間状態共有を回避する設計は良い。落とし穴1（block要素開始）の回帰ガード（`not.toContain("&lt;a")`）を成功ケースに固定した追いコミット（0cfb37c）も適切
3. **検証コマンド: 合格** — レビュー時に実行し `npm run check`（format:check / lint / typecheck / test 49件）green、`npx astro build` 成功（3ページ）を確認
4. **ドキュメント反映: 合格** — link-card.md へ雛形からの逸脱3点・内部リンク除外ガード・「成功のみキャッシュ」による落とし穴3の解消・「コレクション経由実ビルド未検証」残課題の解消を反映済みで、「実装は `plugins/link-card.mjs` を正とする」と明記。phase-2 実施履歴に検証手順（到達不能URL一時投入→revert）まで記録され再現可能。`.prettierignore` への `.cache` 追加も理由コメント付き（§1「執筆済みプローズ・生成物は対象外」の趣旨に沿う）。SSoT違反の重複なし
5. **コミット規約: 合格** — 4コミットとも Conventional Commits + 日本語サブジェクト + `Task: T2-3` トレーラー。type（feat/test/docs）の使い分け・意味のある単位での分割も適切
6. **コード品質: 合格** — `// @ts-check` + JSDoc型注釈（`any`なし・`unknown`+絞り込み）、named exportのみ、kebab-caseファイル名、コメントは「なぜ」（成功のみ保存の理由・defense-in-depthの位置づけ・mermaidと方針が逆である旨）を日本語で記述。ファクトリ閉包によるキャッシュ保持は wikilink.mjs の既存パターンと一貫。`escapeHtml` によるHTML属性値エスケープ・`new URL(url).host` のtry/catchフォールバック・`extractMeta` の property/name両対応+属性順不同など基本的な堅牢性は確保されている。下記の推奨・軽微指摘あり

## 指摘一覧

### 要修正

なし。

### 推奨

- **R-1: fetchにタイムアウトがなく、応答しないサーバがビルドを無期限に停止させうる**（`plugins/link-card.mjs:47`）— `fetch(url, { redirect: "follow" })` にタイムアウトがない。接続拒否やDNS失敗は即座にthrowしてフォールバックするが、**応答を返さずコネクションを保持するサーバ**に当たるとvisitorがawaitのまま進まず、ローカル/CIのビルドが失敗ではなくハングする（R-4「fetch失敗はビルドエラーにしない」の趣旨はフォールバックで満たしているが、ハングはフォールバック経路にすら入らない）。`fetch(url, { redirect: "follow", signal: AbortSignal.timeout(10_000) })` の1行で対処でき、タイムアウトはAbortErrorとしてthrowされるので既存のcatch→フォールバックがそのまま効く。本タスクで直すか、少なくとも link-card.md の「制約・残課題」に追記しておくこと（現状の残課題リストは正規化・文字コード・巨大HTML等を挙げているがタイムアウトは漏れている）

### 軽微

- **R-2: 取得したtitle/descriptionのHTML実体参照が二重エスケープされる**（`plugins/link-card.mjs:52-64`）— `<title>Foo &amp; Bar</title>` のようにソースHTML側でエスケープ済みの文字列をそのまま `escapeHtml` に通すため、出力が `Foo &amp;amp; Bar`（表示上 `Foo &amp; Bar`）になる。コミット済みキャッシュの実データ4件では非発生だが、`&`を含むタイトルのサイトで表示が崩れる。OGPパーサ強化（残課題に記載済みの「`<meta>`属性バリエーション等」）の際に実体参照デコードを合わせて入れるとよい
- **R-3: スキーム判定が小文字限定**（`plugins/link-card.mjs:146`）— `/^https?:\/\//` は `HTTPS://EXAMPLE.COM/` のような大文字スキーム（URLとして有効）にマッチせずカード化されない。安全側（fetchしない方向）への逸脱であり、rule.mdの記法例も小文字のため実害はほぼないが、意図的な制限ならガードのコメントに一言あると次の読者が迷わない
- **R-4: og:content内の連続改行でHTMLブロックが分断されうる**（`plugins/link-card.mjs:30-38`）— `extractMeta` の `content=["']([^"']*)["']` は改行を含む値を許容し、値に空行（連続改行）が含まれるとrawHtml再パース時にHTMLブロック（type 6）が空行で終了してカードHTMLの後半が生markdown扱いになる。極端なエッジケースであり、残課題「巨大HTML等のエッジケースは本番で強化」の範疇として軽微とする（対策する場合はnonEmptyでの改行→空白正規化で足りる）

## 総合判定

**承認**。DoD全6項目を満たしており、要修正の指摘はない。推奨R-1（fetchタイムアウト）は1行で入る堅牢性改善のため、次にこのプラグインへ触れるタイミング（遅くともT2-5の全プラグイン同時動作確認）までの対応を推奨する。
