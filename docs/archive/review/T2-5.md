# T2-5 レビュー: 全プラグイン同時動作確認

- レビュー日: 2026-07-09
- 対象コミット（`Task: T2-5` トレーラー確認済み・誤ヒットなし）:
  - `5cc0e39` feat: rule.md全記法のテスト用記事を追加し既定ビルドで恒常検証可能にする
  - `5c3617d` test: 全プラグイン同時登録の統合テストを追加（wikilink×linkCard非カード化）
  - `05050ec` docs: T2-5の同時動作検証結果を反映（残課題の検証済み化・実施履歴・P2完了）

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | 合格 | 完了条件は「rule.md全記法を含むテスト用mdが1ページで正しくレンダリングされる」「wikilinkがカード化されないことを確認済み」の2点（plan由来。specの番号付きACは参照していない）。①`content/articles/markdown-notation-test.md` をrule.mdの全セクション（見出し1〜4・リスト3種・テキストリンク・リンクカード・辞書リンク（本文中/単独段落）・画像・figure・テーブル・コードブロック（素/ファイル名/diff）・引用・注釈2種・区切り線・インラインスタイル4種・HTMLコメント・message通常+6種+タイトル付き・details・ネスト・mermaid）と突き合わせ、**欠落なし**を確認。レビュー時にContent Layerキャッシュ削除後の実ビルドdistで主要マーカーを独立検証: `/dict/`アンカーの`link-card`クラス**0件**・`ogp.json`に`/dict/`キー**0件**（核心）、`code-filename`+`data-language="rust"`、`has-diff`/`diff add`/`diff remove`、`--shiki-light`/`--shiki-dark`両方、mermaid figure（`&lt;svg`0件・distにmermaidランタイム非配布）、figureの`width="480"`反映+figcaption、canary`12:30`残存、link-cardはフォールバックなし（`https://doc.rust-lang.org/cargo/`はT2-3時点でキャッシュ済み＝ネットワーク非依存の主張も正確）。②統合テストでwikilink非カード化+fetch非発生を検証。仕様にない実装の混入なし |
| 2 | テスト | 合格 | `tests/plugins/pipeline.test.ts` 5ケース + `tests/helpers/pipeline.ts`。§5準拠: `tests/` ミラー配置・`.test.ts`・日本語it名・AAA・fetchは`vi.stubGlobal`スタブ（全ケースで実ネットワーク遮断）・mermaidはfakeレンダラDI（Chromium非起動）・scratch cacheDirで実キャッシュ非汚染。ヘルパの登録順（codeFilename → wikilink → directives → linkCard / hast: mermaid）が本番`astro.config.mjs:51,54`と一致することを確認。`[AC-n]`タグなしはplan由来完了条件（spec AC非対応）のためT2-1〜T2-4の前例どおりで妥当。`features.directive`なしだと誤greenになる旨のコメント・Shiki非走行の分担コメントも適切 |
| 3 | 検証コマンド | 合格 | レビュー時に実行: `npm run check`（format:check / lint / typecheck / test **66件**）green、`npx astro build`（キャッシュ削除後）成功 |
| 4 | ドキュメント反映 | 合格 | markdown-pipeline/README.md・link-card.md の「同時動作未検証」を検証済みへ更新（検証手段の詳細はlink-card.mjs側に置きREADMEは要約+リンクでSSoT維持）、wikilink.md に相互参照1行追加、plan/README.md のP2状態を完了へ、phase-2実施履歴に検証内容・HTMLコメント許容判断・P6ダミー整理への申し送り（テスト記事は削除せず非公開化・debug-render撤去時の検証経路はP3/P6判断）を記録。T2-2申し送りR-1（ファイル名記法の恒常カバー）の解消も明記。矛盾・重複なし |
| 5 | コミット規約 | 合格 | 3コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T2-5` トレーラー（`git log --grep`で3件のみヒット、誤ヒットなし） |
| 6 | コード品質 | 合格 | named exportのみ・kebab-caseファイル名・`any`不使用（fakeレンダラの型橋渡しは`unknown`経由+理由コメント、§3準拠）・import順（外部→内部）・「なぜ」コメント・日本語コメント、いずれも規約準拠。テストのアサーションも既存流儀（`toContain`/正規表現件数照合）と一貫。`class="link-card"`の件数照合はfallback（`link-card--fallback`複合クラス）と成功カードを区別できており、非カード化・重複カード化の両回帰を正しく検出できる構成 |

## 指摘一覧

### 要修正

- なし

### 推奨

- なし

### 軽微

- **R-1: figureの省略バリアント（キャプション省略・width省略）がテスト記事に未収載**（`content/articles/markdown-notation-test.md:56-59`）
  - rule.mdは「画像サイズやキャプションは省略可能」を明記するが、テスト記事は両方指定の`:::figure[キャプションテキスト]{width=480}`のみ。省略形はT2-1の単体テスト（directives.test.ts）でカバー済みのため実害はないが、「rule.md全記法の恒常dist検証」という記事の趣旨からは省略形（例: `:::figure`のみで囲うパターン）も1つあるとより堅い。次回テスト記事を触る機会での追加で足りる。

## 総合判定

**承認**。完了条件（全記法の1ページレンダリング・wikilink非カード化）は統合テストと実ビルドdistの両経路で満たされており、レビュー時の独立再検証でも齟齬はなかった。軽微R-1は対応任意。
