# T2-4 レビュー: mermaid（ビルド時SVG化）

- レビュー日: 2026-07-09
- 対象コミット:
  - `f2275ce` feat: mermaidコードブロックのビルド時SVG化を組み込む
  - `bb9519a` test: mermaidプラグインのユニットテストを追加
  - `d95599f` docs: T2-4（mermaid）の実施履歴とmermaid.md本番化事項を記録

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | **不合格** | 「2枚のSVGに変換・id重複なし・mermaid.js非配布」は文字列レベルでは満たすが、`namespaceSvgIds` がSVGルートidを書き換える一方で `<style>` 内のCSSセレクタ（`#<ルートid> …`）を書き換えないため、**mermaidのテーマスタイル全ルールが死にセレクタ化し図に一切適用されない**（要修正R-1）。ライト/ダーク2枚を作る目的（テーマ配色）が実質機能しない |
| 2 | テスト | 合格（軽微あり） | `tests/plugins/mermaid.test.ts` 8ケース。純関数テスト+fakeレンダラDIで§5準拠（実ブラウザ非起動・AAA・日本語it名・throwメッセージ検証）。ただし `[AC-mermaid]` タグは§5の `[AC-n]`（spec AC番号）規約外（軽微R-3）。またfake SVGに `<style>` が無いためR-1を検出できない構成だった |
| 3 | 検証コマンド | 合格 | レビュー時に実行: `npm run check`（format:check / lint / typecheck / test 59件）green、`npx astro build` 成功 |
| 4 | ドキュメント反映 | 合格（R-1修正時に要追随） | mermaid.md「本番化（T2-4）での確定事項」追記・実ビルド残課題の解消反映・phase-2実施履歴とも整合。SSoT違反なし。ただし落とし穴1の「部分一致の誤置換は起きない」検証はstyle内セレクタを見落としており、R-1修正時に落とし穴へ追記が必要 |
| 5 | コミット規約 | 合格 | 3コミットとも Conventional Commits + 日本語サブジェクト + 正確な `Task: T2-4` トレーラー |
| 6 | コード品質 | 合格（R-1除く） | `@ts-check`+JSDoc型注釈・`any`不使用（`getLang` で型橋渡し）・named exportのみ・kebab-caseファイル名・「なぜ」コメント・`{ cause }` 付きthrow・エラーメッセージの `絶対パス:行:列` 形式、いずれも規約準拠 |

## 指摘一覧

### 要修正

- **R-1: `namespaceSvgIds` がid属性を書き換える一方、SVG内 `<style>` のCSSセレクタを書き換えず、mermaidのテーマスタイルが全滅する**（`plugins/mermaid.mjs:36-59`）
  - mermaidが吐くSVGは `<style>#<ルートid>{font-family:…;fill:#333;} #<ルートid> .node {…} …</style>` と、**全スタイルルールをルートidセレクタでスコープ**している。`namespaceSvgIds` は `id="…"` / `url(#…)` / `href="#…"` / `aria-*` のみ書き換えるため、ルートidが `mmd0l-0` → `mmd0l-mmd0l-0` に変わった後もセレクタは `#mmd0l-0` のまま残り、どの要素にもマッチしない。
  - 実レンダラで実証（flowchart TD / A --> B）: 書き換え後、style内に取り残しセレクタ `#mmd0l-0` が**71箇所**、書き換え後id `#mmd0l-mmd0l-0` への言及は**0箇所**。全59ルールが死にCSS化。結果、図はノード色・エッジ色・フォント等が全てブラウザ既定になり、light/dark 2枚の配色差（本機能の眼目）がブラウザ上で消える。
  - 実施履歴の実ビルド検証（distのgrep）は文字列レベルの確認のためこれを検出できない（mermaid.md残課題「切替実表示は未目視」の通り視覚確認は未実施であり、検証手順自体の落ち度ではない）。
  - 修正案（いずれか）:
    1. **ルートidを書き換え対象から除外する**。ルートidはmermaidの `prefix`（`mmd{index}{l|d}`）で既にper-SVG一意であり、light/dark間で衝突しない。除外すればセレクタと一致が保たれる（最小修正）。
    2. `<style>` 内の `#<id>` セレクタも書き換え対象に加える（網羅的だがセレクタ文法の誤置換リスク管理が必要）。
  - あわせて: fakeレンダラのSVGに `<style>#<prefix由来id>…</style>` を含め、「style内セレクタとid属性が書き換え後も一致する」回帰テストを追加すること。mermaid.md落とし穴1（「誤置換は起きない」）と「出力構造・検証結果」への追記も必要。

### 推奨

- なし

### 軽微

- **R-2: `[AC-mermaid]` は§5のAC対応付け規約（`[AC-n]`=spec AC番号）に沿わない独自タグ**（`tests/plugins/mermaid.test.ts:29,66,109`）
  - T2-4の完了条件はplan由来でspecの番号付きACが存在しない（theme.md AC-4はP6範疇）。同状況のT2-1〜T2-3はプレフィックスなしが前例。grepでspecに追跡できないタグは規約の意図（spec⇔テストの対応追跡）に合わないため、削除するか前例に合わせるのが望ましい。
- **R-3: `aria-labelledby` / `aria-describedby` がスペース区切りの複数id値の場合に追従しない**（`plugins/mermaid.mjs:50-53`）
  - 正規表現が `="<id>"` の完全一致アンカーのため、`aria-labelledby="a b"` 形式では両idとも書き換えられずdangling参照になる。今回の実測出力（flowchart）にはaria属性自体が現れず実害なし。mermaid.md残課題の「他形式は必要になれば追加」と同枠として、残課題への1行追記で足りる。

## 総合判定

**要修正**（R-1）。R-1修正後に `npm run check` / `astro build` を再実行し、再度 `/task-review T2-4` を実施すること。

---

# T2-4 再レビュー: レビューR-1〜R-3反映の確認

- レビュー日: 2026-07-09
- 対象コミット（前回レビュー後の追加分。`Task: T2-4` トレーラー確認済み）:
  - `dc3c8bd` fix: mermaidのルートidを名前空間化から除外しテーマCSSの死にセレクタを解消
  - `f4bc662` docs: T2-4レビューR-1修正を反映（ルートid除外の落とし穴・残課題・実施履歴）

## 前回指摘の対応確認

- **R-1（要修正）: 対応済み・実証確認済み**。`namespaceSvgIds` がルートid（`<svg` 開始タグ内の最初の `id`）を書き換え対象から除外するよう修正（`plugins/mermaid.mjs:44-49`）。レビュー時に実レンダラ（Playwright/Chromium・flowchart light/dark）で独立検証:
  - ルートidが `mmd0l-0` / `mmd0d-0` のまま不変で、`<style>` 内の `#<ルートid>` セレクタと一致を維持（真の孤立セレクタ0。検証スクリプトが1件flagしたのは `border:1px solid #aaaa33` の色値の誤検知で、セレクタではない）
  - light/dark間のid積集合は引き続き空
  - 二重前置き（`mmd0l-mmd0l-0_…`）はマーカー/グラデーションidのみで、定義・参照が揃って書き換わるため無害（mermaid.md落とし穴1に記載の通り）
  - 回帰テスト（`orphanStyleSelectors` による死にCSS検出）が純関数・プラグイン経由の両方に追加されている
- **R-2（軽微）: 対応済み**。`[AC-mermaid]` 独自タグを削除しT2-1〜T2-3のプレフィックス無し前例に統一。
- **R-3（軽微）: 対応済み**。aria複数id非追従をmermaid.md残課題へ追記（方針通り記録のみ）。

## DoDチェック結果（再検証）

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | 合格 | R-1解消によりテーマスタイルが適用され、light/dark 2枚SVG・id重複なし・mermaid.js非配布の完了条件を実質面でも満たす |
| 2 | テスト | 合格 | 死にCSS検出を核に据えたテスト構成へ改善（60件）。light/dark別ルートidのフィクスチャで除外ロジックの誤検証（同一rootで衝突を見逃す嘘）も防いでいる |
| 3 | 検証コマンド | 合格 | レビュー時に実行: `npm run check`（test 60件）green、`npx astro build` 成功 |
| 4 | ドキュメント反映 | 合格 | 落とし穴1へのルートid除外の理由・旧注記の偽陰性見落とし・図種依存の再発リスク（残課題）・実施履歴のレビュー反映段落、いずれも適切。SSoT違反なし |
| 5 | コミット規約 | 合格 | 2コミットとも Conventional Commits + 日本語サブジェクト + `Task: T2-4` トレーラー |
| 6 | コード品質 | 合格 | 修正は最小限（レビュー提示の修正案1）で規約準拠 |

## 指摘一覧

### 要修正

- なし

### 推奨

- なし

### 軽微

- **N-1: 「バセレクタ」という不明な用語がコメント・テスト・文書に繰り返し使われている**（`plugins/mermaid.mjs:30,38` / `tests/plugins/mermaid.test.ts:18,65` / `docs/markdown-pipeline/mermaid.md:79,189,190,203,205` / `docs/plan/phase-2-pipeline.md:81`）
  - 「バセレクタ」「バ`#id`セレクタ」は日本語として意味が取れない（「ハッシュ（`#`）セレクタ」ないし単に「`#id`セレクタ」の誤記と推測）。§2言語ポリシー（コメント・ドキュメントは日本語）の趣旨である可読性を損なうため、次回コミット時に「`#id`セレクタ」等へ一括修正が望ましい（コミットメッセージ内の同語は履歴のため修正不能・許容）。
- **N-2: テストヘルパ `orphanStyleSelectors` の色値除外ヒューリスティック（直前が `:`）がショートハンド値を除外できない**（`tests/plugins/mermaid.test.ts:27-29`）
  - `border:1px solid #aaa` のように色値の直前が `:` でないCSSでは色値をセレクタと誤判定する。実mermaid出力のflowchart CSSにはまさに `border:1px solid #aaaa33` が含まれるため、残課題に書かれた「図種を広げる際は孤立ゼロを再確認」を実SVGに対して行うと誤検知する。現状のfakeフィクスチャ（`fill:#333` 形式のみ）では実害なし。セレクタ判定を「`{` の手前（セレクタ位置）に現れる `#id` のみ」へ絞る等の強化が望ましい。

## 総合判定

**承認**。要修正・推奨の指摘なし（軽微2件は次回コミットでの対応で足りる）。
