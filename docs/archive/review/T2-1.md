# T2-1 レビュー: directives + textDirective復元

- レビュー日: 2026-07-09
- レビュアー: Claude Fable 5（/task-review）

## 対象コミット

| ハッシュ | サブジェクト |
| --- | --- |
| 00fe233 | feat: directivesプラグインを追加しパイプラインに組み込む |
| 8b43367 | test: directivesプラグインのテストを追加 |
| ccf57a4 | docs: T2-1の実施結果を記録しmessage併用パターンの実測を反映 |
| 2c99ccc | fix: figureのキャプションを省略可にしrule.mdに合わせる |

4コミットとも本文に正確な `Task: T2-1` トレーラー行を確認済み（誤ヒットなし）。範囲diff `git diff 00fe233^..2c99ccc` で全体を確認した。

## DoDチェック結果

1. **完了条件・AC充足: 合格** — directiveに対応する `docs/spec/` の受入基準は存在せず（grepで確認）、完了条件はplan文書（phase-2 T2-1）が定める「rule.mdの全directive記法が正しいHTML（aside/details/figure）に変換され、本文中の`x:y`が消えない。vitestが通る」。`plugins/directives.mjs` は rule.md の全記法（message 6種別+種別なし+`[タイトル]`、details `[タイトル]`必須、figure キャプション省略可+width、ネスト）を directives.md の検証済み方式どおりに実装しており充足。当初雛形由来でfigureキャプション必須（rule.md違反）だったが、2c99cccで省略可に修正済み。仕様にない実装の混入なし（class shorthand不採用・デフォルトタイトル未実装は directives.md の決定どおり）
2. **テスト: 合格（注記あり）** — `tests/plugins/directives.test.ts` 12ケースが §5 の規約（`tests/` ミラー配置・`.test.ts`・日本語it名・AAA・`tests/helpers/` の共通ヘルパー・エラー系は `toThrow(/日本語断片/)`）に従う。**`[AC-n]` 命名はspecにdirectiveのACが存在しないため対象外**（完了条件はplan文書由来。既存wikilinkテストのHTML `toContain` スタイルとも一貫）
3. **検証コマンド: 合格** — レビュー時に実行し `npm run check`（format:check / lint / typecheck / test 36件）green、`npx astro build` 成功（3ページ）を確認
4. **ドキュメント反映: 合格（軽微な不整合あり、指摘R-2/R-3）** — 未実測だった `:::message[タイトル]{info}` の実測結果と figureキャプション省略可の本番挙動を directives.md へ還流済み。phase-2 実施履歴も記載あり。SSoT違反の重複なし（実施履歴のrule.md引用は判断根拠の記録として許容範囲）
5. **コミット規約: 合格** — 4コミットとも Conventional Commits + 日本語サブジェクト + `Task: T2-1` トレーラー。type（feat/test/docs/fix）の使い分けも適切、意味のある単位で分割されている
6. **コード品質: 合格** — `// @ts-check` + JSDoc型注釈、named exportのみ、kebab-caseファイル名、`any` なし、コメント・エラーメッセージ日本語、throwメッセージは「ファイル:行:列」形式（satteri-plugin-api準拠）。`setProperty` でのdata丸ごと置換時に `directiveLabel: true` を含め直す落とし穴、UTF-8バイトオフセットのBufferスライス、ネスト2回訪問（自ノードのみ変換）もすべて正しく回避されている

## 指摘一覧

### 要修正

なし。

### 推奨

- **R-1: phase-2実施履歴の冒頭段落が最終実装と矛盾**（`docs/plan/phase-2-pipeline.md:29`）— 「details/figure はlabel必須とし、labelが無ければthrow」とあるが、2c99cccでfigureはlabel省略可（figcaptionなしの`<figure>`）に修正済み。同節の要注意点bullet（33行目）とコードは正しいが、要約段落だけが修正前の記述のまま残っており、履歴を読む将来のセッションが誤読するリスクがある。「detailsはlabel必須でthrow、figureのlabelは省略可」へ修正すべき
- **R-2: messageの未知属性タイポが黙って無視される**（`plugins/directives.mjs:80`）— `MESSAGE_TYPES.find((t) => t in attrs)` は `:::message{warnig}` のようなタイポで `undefined` になり、種別なしの `class="message"` として静かにレンダリングされる。directives.md の設計思想（「タイポが黙って消えるのを防ぐ」ために未知directive名はthrow）と非対称。本文が消えるわけではないため要修正とはしないが、未知属性をthrowで検出するか、「属性タイポは検出しない」旨を directives.md の制約・残課題へ明記するかを次タスク以降で決めることを推奨

### 軽微

- **R-3: 実施履歴のテスト件数が古い**（`docs/plan/phase-2-pipeline.md:36`）— 「test 34件」はccf57a4時点の値。2c99cccでテストが2件追加され現在は36件（レビュー時実測）。修正時にR-1と併せて更新するとよい

## 総合判定

**承認**（要修正なし。推奨2件・軽微1件は次回のドキュメント更新・後続タスクで対応すればよい）
