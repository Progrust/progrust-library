# T3-6 レビュー: タグ一覧・タグ詳細・プロフィール・404

- **レビュー日**: 2026-07-10
- **対象コミット**:
  - `ed9ea2b` feat: タグ詳細の純ロジック tagLedger/TagLedgerItem を追加
  - `e7d40a7` feat: タグ一覧・タグ詳細ページを実装
  - `2d9374e` feat: プロフィール・404ページを実装
  - `f8d5e75` docs: T3-6の実施履歴を追記しP3を完了に更新

## DoDチェック結果

| # | 観点 | 判定 | 根拠 |
| --- | --- | --- | --- |
| 1 | 完了条件・AC充足 | ✅ | AC-1: build で 92 ページ生成（`/tags/index.html`・各 `/tags/[タグ]/index.html`・`/profile/index.html`・`/404.html` を生成物で確認）。AC-6: `dist/tags/Web開発/index.html` に章行「Rustで始めるWeb API開発 - 実践ガイド › イントロダクション」が「本」バッジ付きで存在。AC-8: `dist/404.html` に BaseLayout のヘッダー/フッターと「トップへ戻る」（`href="/"`）。R-18（全タグ・件数付き・件数降順/同数タグ名昇順 = `tagCounts` 既存実装）、R-19（種別混在 created_at 降順・R-4 タイブレーク・章併記・「本」バッジ・戻る導線）、R-20（profile.html モック転記・BaseLayout 供給のヘッダー/フッター）、R-21 を充足。仕様外の実装なし（タグ詳細の「他タグ2件」表示は確定モック tags-detail 準拠） |
| 2 | テスト | ✅ | `tests/lib/content.test.ts` に `tagLedger` の describe を追加。`[AC-6]` 命名（章 kind:"book" の混入）+ 降順・タイブレーク・非該当除外・tags 保持の計5件。AAA・`toMatchObject` 部分一致・日本語 it 名、§5 の配置（`tests/lib/`）に適合 |
| 3 | 検証コマンド | ✅ | レビュー時に実行: `npm run check`（format:check / lint / typecheck 0 errors / vitest 89 tests）PASS、`npx astro build` 成功（92 ページ） |
| 4 | ドキュメント反映 | ✅ | `docs/plan/phase-3-pages.md` に実施履歴（実装内容・申し送り・検証結果）、チェックボックス更新。P3 全タスク完了に伴い `docs/plan/README.md` のフェーズ状態を「完了」へ。日本語タグの percent-encoding 整合（T3-4 M-4 申し送りの解消）も履歴に記録。SSoT 違反の重複記載なし |
| 5 | コミット規約 | ✅ | 4 コミットとも Conventional Commits（feat/docs）+ 日本語サブジェクト + 正確な `Task: T3-6` トレーラー |
| 6 | コード品質 | ✅ | named export のみ・`any` なし・命名/言語ポリシー準拠。`TagLedgerItem`/`tagLedger` に日本語 JSDoc。Astro コンポーネント規約（Props interface・スクリプトなし・`<style>` なし・トークン CSS 変数方式）準拠。軽微指摘は下記 |

## 指摘一覧

### 要修正

なし。

### 推奨

なし。

### 軽微

- **M-1: R-4 タイブレークロジックの重複** — `src/lib/content.ts` の `tagLedger()`（サブジェクト: created_at 降順 + title 昇順）は既存 `sortByNewest()` と同一の比較ロジックを再実装している。`TagLedgerItem` がフラット構造（`data.` なし）のため流用できない形になっているが、R-4 の定義が 2 箇所に分かれるため、将来タイブレーク仕様が変わると片方の直し漏れが起こりうる。比較関数の共通化（`compareNewest(aDate, aTitle, bDate, bTitle)` 等の切り出し）を検討。現状はテストで両方カバーされており実害なし。
- **M-2: SNS アイコン SVG パスの重複** — `src/pages/profile.astro` の GitHub/X/RSS のパスデータは `src/pages/index.astro`（ヒーロー）と同一文字列の重複。R-20 が「profile.html モックの直書き転記」を指示しているため今回の方針としては妥当だが、3 箇所目が生まれるなら `SocialIcon` 的な部品化を検討。
- **M-3: タグ名の URL 文字制約が未文書化** — タグ名に `/` を含むと `[tag]`（単一セグメントルート）の生成パスとリンク側 `encodeURIComponent`（`%2F`）が不整合になりうる。T3-3 申し送りの `|` 区切り問題（`data-tags`）と合わせ、content-model.md にタグ名の文字制約（`/`・`|` 禁止等）を追記するのが安全。現ダミーコンテンツに該当タグはなく実害なし。

## 総合判定

**承認**。AC-1 / AC-6 / AC-8 を生成物レベルで確認、DoD 全項目クリア。軽微 3 件はいずれも既存パターン踏襲・仕様準拠の範囲内で、対応は任意（M-3 は P5 の絞込実装時に T3-3 M-4 と併せて対応するのが自然）。

---

# T3-6 再レビュー（M-1 対応の確認）

- **レビュー日**: 2026-07-10
- **対象コミット**（前回レビュー後の追加分。`Task: T3-6` トレーラー確認済み）:
  - `3726ca3` refactor: 新着順の比較を compareNewest に共通化

## 確認結果

前回指摘 **M-1**（R-4 タイブレークロジックの重複）への対応コミット。`src/lib/content.ts` に `compareNewest()`（created_at 降順・同日 title 昇順）を切り出し、`sortByNewest()`（`data.` 付き構造）と `tagLedger()`（フラット構造）の両方から利用する形に集約された。R-4 の定義が一箇所になり、指摘の趣旨どおりの解消を確認。

- **振る舞い不変**: 既存テスト（`sortByNewest`/`mergeRecent` の R-4 系・`tagLedger` の降順/タイブレーク）が比較ロジックを両経路でカバーしており、全件 PASS
- **規約適合**: named export・日本語1行JSDoc（「なぜ」= R-4 集約の意図を記載）・camelCase 命名、`refactor:` + 日本語サブジェクト + `Task: T3-6` トレーラー（§2/§3/§6）に適合
- **検証コマンド**（再実行): `npm run check`（vitest 89 tests）PASS / `npx astro build` 成功（92 ページ）

## 指摘一覧

### 要修正 / 推奨

なし。

### 軽微

- **M-4: 実施履歴にレビュー対応の追記がない** — `docs/plan/phase-3-pages.md` の T3-6 実施履歴に本リファクタ（M-1 対応）の記載がない。T3-3 では「再レビュー対応」段落を履歴に残しており、慣行に合わせるなら1行追記が望ましい（対応は任意。コミットメッセージとレビュー文書で追跡は可能）。

前回の M-2（SVG パス重複）・M-3（タグ名の URL 文字制約未文書化）は対応任意のため未対応のままで問題なし（M-3 は P5 で対応予定）。

## 総合判定

**承認**（維持）。M-1 は解消。残指摘は軽微のみで対応任意。
