# T1-1 レビュー結果

- **レビュー日**: 2026-07-09
- **総合判定**: ✅ **承認**（要修正なし）

## 対象コミット

4コミットすべてに正確なトレーラー `Task: T1-1` を確認済み（`T1-10` 等への誤ヒットなし）。

| ハッシュ | サブジェクト |
| --- | --- |
| ad55b3b | chore: Astroプロジェクトを初期化しツールチェーンを整備 |
| 55d9cd0 | chore: ダミーコンテンツをcontent/へ移植 |
| 072f8bb | chore: ESLintの.mjs設定を修正しローカル設定を除外 |
| a6e2b99 | docs: phase-1にT1-1の実施メモと申し送りを追記 |

※ ad55b3b はルートコミットのため、範囲diffはリポジトリ全体と同一。

## DoDチェック結果（implementation-rules §7 / §6）

### 1. 完了条件・AC充足 — ✅ 合格

完了条件「`astro dev` / `astro build` が成功し、`npm run check` が通る（vitestは空テストで可）」をレビュー時に独立検証し、すべて成功を確認:

- `astro dev`: 起動し `http://localhost:4399/` が 200 を返す
- `astro build`: 成功（1ページ生成）
- `npm run check`: format:check / lint / typecheck / test すべて通過

コンテンツ移植の検証:

- `content/` は content-model **R-3** の正規構成（`dict/` `articles/` `books/[slug]/index.md+章`）に準拠。`diff -r` で原本 `docs/ui-design/dummy-contents/` と完全一致（申し送りの「コピー・無修正」記載と整合）
- frontmatter を各コレクションでスポットチェックし [frontmatter.md](../../markdown-notation/frontmatter.md) の定義（title/description/created_at/updated_at/tags/public、articles・books の image）に準拠
- 申し送りの「コードフェンス外の wikilink はリンク切れ0件」をスクリプトで独立検証: dict 25エントリに対し broken 0件。`rust-performance-tuning.md` の ```` ```toml ```` 内 `[[bench]]` がフェンス除外前提で誤検出とならないことも確認
- dict 配下のファイル名重複なし（R-6 の前提を満たす）
- ツールチェーンは implementation-rules §1 のとおり: Prettier+prettier-plugin-astro / ESLint flat config（js・tseslint・astro の recommended、`plugins/**/*.mjs` に `globals.node`）/ `astro/tsconfigs/strict`（緩和なし）/ vitest / 固定 npm scripts（`check` 含む）
- 仕様にない実装の混入なし（`index.astro` は P1 検証用プレースホルダーと明記、Sätteri は P2 送りで計画どおり）

### 2. テスト — ✅ 合格

完了条件が「vitestは空テストで可」と明記しており、`tests/smoke.test.ts` の疎通テストで充足。it名は日本語の振る舞い文（§5準拠）。AC由来の `[AC-n]` テストは T1-2 以降で追加する旨がテスト内コメントにも明記されている。

### 3. 検証コマンド — ✅ 合格

`npm run check` と `npx astro build` をレビュー時に実際に実行し、両方成功（観点1に詳細）。

### 4. ドキュメント反映 — ✅ 合格（推奨1件）

phase-1 に実施メモと申し送り（バージョン固定理由・Prettierスコープ・ESLint .mjs 対応・wikilink検証のコードフェンス除外・移植はコピー）が記録されている。特に `[[bench]]` の誤検出リスクの申し送りは T1-3/T1-4 に有効。SSoT違反となる重複記載は見当たらない。推奨1（下記）のみ。

### 5. コミット規約 — ✅ 合格

4コミットとも Conventional Commits + 日本語サブジェクト + `Task: T1-1` トレーラー（§6準拠）。type は `chore` / `docs` で妥当。`.prettierrc` のオーバーライド理由もコミット本文で説明されている（§1の要求）。

### 6. コード品質 — ✅ 合格

- 言語ポリシー（§2）準拠: 識別子は英語、コメントは日本語
- default export は Astro 設定ファイル（`astro.config.mjs` / `eslint.config.js` / `vitest.config.ts`）のみで、これらは各ツールが default export を要求する設定ファイルであり §3 の禁止対象（自作モジュール）に当たらない
- `any` なし。コメントは「なぜ」を説明する内容（tseslint 非type-aware選択の理由、`.prettierignore` のスコープ理由等）で質が高い
- `tsconfig.json` は strict extends のみで緩和なし

## 指摘一覧

**要修正**: なし

**推奨**:

1. **Prettierスコープ方針を implementation-rules §1 へ反映** — 「Prettier の対象はプロジェクトが所有するコードに限定し、執筆済みプローズ（`content/` `docs/` 等）は対象外」という方針が、現状 `.prettierignore` のコメントと phase-1 申し送りにのみ存在する。§1 は自らを「ツール選定と設定方針のSSoT」と定めているため、この方針（設定ファイルの中身の複製ではなく方針1行）を §1 に追記するのが SSoT 上望ましい。該当: `.prettierignore`、`docs/implementation-rules.md` §1

**軽微**:

1. `eslint.config.js:8` — `tseslint.config()` が typescript-eslint 8.63 で deprecated（`astro check` が hint として報告）。動作に支障はないが、将来 `defineConfig` 形式への移行を検討
2. `.claude/settings.local.json` が ad55b3b で一度コミットされ 072f8bb で削除されたため git 履歴に残る。中身は権限許可リストのみで機密情報は含まれておらず実害なし（記録のみ）
3. インストール版 astro 7.0.7 は markdown-pipeline README の検証済み 7.0.6 と同一パッチ系列であり、計画の「検証済みバージョン系」に合致。申し送りにも記録済み（問題なし、確認記録のみ）

## 総合判定

**承認**。完了条件・DoD の全項目を満たしており、要修正の指摘はない。推奨1（Prettierスコープ方針の §1 反映）は後続タスクの冒頭などで対応すればよい。

---

# T1-1 再レビュー結果（指摘対応の確認）

- **レビュー日**: 2026-07-09
- **対象**: 初回レビューの推奨1・軽微1への対応コミット
- **総合判定**: ✅ **承認**（対応完了を確認）

## 対象コミット

| ハッシュ | サブジェクト | 対応する指摘 |
| --- | --- | --- |
| 66eb2c4 | docs: Prettier整形スコープの方針をimplementation-rules §1へ反映 | 推奨1 |
| 062435e | refactor: ESLint設定をdefineConfigへ移行しdeprecation解消 | 軽微1 |

いずれも正確な `Task: T1-1` トレーラー付き。

## 確認結果

### 推奨1: Prettierスコープ方針の §1 反映 — ✅ 解消

- `implementation-rules.md` §1 のフォーマッタ行に「整形対象はプロジェクト所有コードに限定し、執筆済みプローズは `.prettierignore` で対象外」の方針が理由付きで追記された
- phase-1 の申し送りは §1 への参照（「方針の正は §1」）に書き換えられ、**重複記載も同時に解消**されている。SSoT 原則に照らして期待どおりの対応

### 軽微1: `tseslint.config()` deprecation — ✅ 解消

- ESLint 標準の `defineConfig`（`eslint/config`）へ移行。`astro check` の再実行で **0 errors / 0 warnings / 0 hints** を確認（ts(6387) hint が消滅）
- 挙動維持を独立検証: `eslint .` は exit 0、`--print-config` で `plugins/**/*.mjs` に `globals.node`（`process` 等）が引き続き適用されることを確認

### 検証コマンド — ✅ 合格

`npm run check`（format:check / lint / typecheck / test）と `npx astro build` を再実行し、すべて成功。

### コミット規約 — ✅ 合格

2件とも Conventional Commits + 日本語サブジェクト + `Task: T1-1` トレーラー準拠。type（`docs` / `refactor`）も内容に合致。

## 残指摘

- 軽微2（`settings.local.json` の履歴残存）・軽微3（astro 7.0.7 と検証済み 7.0.6 の差）は記録のみで対応不要としたもの。残る要対応事項はなし。
